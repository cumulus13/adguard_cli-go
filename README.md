# AdGuard Home CLI (Go)

A high-performance, cross-platform command-line tool written in Go to interact with AdGuard Home DNS rewrite rules.

## Installation & Build

### Prerequisites
- Go 1.22+

### Direct Build
```bash
go build -o adguard-cli main.go